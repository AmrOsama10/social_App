export enum SYS_ROLE {
  user,
  admin,
  superAdmin,
}
export enum GENDER {
  male,
  female,
}
export enum USER_AGENT {
  local,
  googl,
}

export enum REACTION {
  like,
  love,
  care,
  sad,
  angry,
}

export enum STATUS {
  active,
  frozen,
  deleted,
}

export enum USER_RELATION {
  normal,
  blocked,
}
